DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/vibe_kanban?schema=public"
JWT_SECRET="change_me"
JWT_EXPIRES_IN="1d"

# OTP / password reset
OTP_TTL_MINUTES="10"
JWT_PASSWORD_RESET_EXPIRES_IN="15m"

# App
PORT="3000"
NODE_ENV="development"

# Development only: return OTP in API response (never enable in prod)
RETURN_OTP_IN_RESPONSE="true"

